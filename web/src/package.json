{
  "name": "ts",
  "version": "0.0.1",
  "description": "TypeScript package for docker monitor",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "dev": "esbuild ./*.ts --bundle --outdir=../static/js --watch",
    "build": "esbuild --bundle --minify --outdir=../static/js ./*.ts",
    "build:css": "sass ./styles/main.scss ../static/css/main.css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "esbuild": "^0.27.3",
    "typescript": "^5.9.3"
  }
}
